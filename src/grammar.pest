text = { 
    (!("<") ~ ANY)+
}
tag_name = {
    (ASCII_ALPHA | "_") ~ (ASCII_ALPHANUMERIC | "_")+
}
attribute_name = {
    (ASCII_ALPHA | "_") ~ (ASCII_ALPHANUMERIC | "_" | "-")+
}
attribute_value = {
    !("\"" | "\'") ~ (ANY)+
}
